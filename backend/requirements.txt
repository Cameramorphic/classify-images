# To ensure app dependencies are ported from your virtual environment/host machine into your container, run 'pip freeze > requirements.txt' in the terminal to overwrite this file
flask==1.1.1
gunicorn==19.7.1 #this gunicorn version is important because newer versions threw "transport endpoint not connected" errors after some days
scipy==1.5.2
requests==2.24.0
werkzeug==1.0.1
pymupdf==1.17.6
numpy==1.19.2
pandas==1.1.2
openpyxl==3.0.5
XlsxWriter==1.3.6
ordered-set==4.0.2
docx-text==0.1.2
opencv-python-headless #this version of opencv is for servers/dockers that don't have the gui packages necessary for the normal package installes
requests_toolbelt